# cargo-features = ["resolver"]

[workspace]
members = [
  "core",
  "cli",
  "gui",
  "gui-druid",
  "gui-vgtk",
  "league",
  "storage",
]
# resolver = "2"

[profile.release]
lto = true
panic = 'abort'
